// exemplo com arrays 

notas = [8,9,7,3]

notas.pop(); // remove o ultimo elemento do array
console.log(notas);

notas.push(5); // adiciona a nota 5 na array
console.log(notas);
console.log(notas.pop());
console.log(notas);
